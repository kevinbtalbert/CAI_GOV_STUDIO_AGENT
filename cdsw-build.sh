# Currenly this build file is used just to set up basic packages for workflow deployment.
# Tool specific packages are installed in the workflow deployment script during runtime.
pip install .[workflow_deployment]
